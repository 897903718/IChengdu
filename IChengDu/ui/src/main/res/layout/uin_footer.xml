<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="60dp">

        <ProgressBar
            android:id="@+id/footer_progressbar"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_centerInParent="true"
            android:indeterminate="false"
            android:indeterminateDuration="1000"
            android:indeterminateDrawable="@drawable/uin_pull_to_refresh_loading"
            android:visibility="invisible"/>

        <TextView
            android:id="@+id/footer_hint_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="@string/footer_hint_load_normal"/>

        <ImageView
            android:id="@+id/footer_arrow"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginRight="30dp"
            android:layout_marginEnd="30dp"
            android:layout_toLeftOf="@id/footer_hint_text"
            android:layout_toStartOf="@id/footer_hint_text"
            android:contentDescription="@null"
            android:src="@drawable/uin_ic_loading"
            android:visibility="gone"/>
    </RelativeLayout>
</RelativeLayout>
